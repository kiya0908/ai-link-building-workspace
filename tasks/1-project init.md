You are building a brand new Chrome Extension project from scratch.

DO NOT modify my existing extension project.

Create a completely new project folder named:

ai-link-building-workspace

Tech stack requirements:

* Manifest V3
* WXT
* React
* TypeScript
* Zustand
* IndexedDB
* OpenRouter API integration
* Injected sidebar architecture
* Content script based DOM interaction

IMPORTANT ARCHITECTURE REQUIREMENTS:

* Use provider-based DOM abstraction
* Use modular scalable architecture
* Use interface-first TypeScript design
* Queue state must persist in IndexedDB
* Avoid hardcoded business logic in querySelector
* DOM operations must be isolated in providers
* AI provider must be replaceable
* Extension must support future comment systems

Initial goal:

ONLY initialize the full project architecture and folder structure.

DO NOT implement business logic yet.

Expected setup:

* WXT extension structure
* React sidebar app
* IndexedDB setup
* Zustand store setup
* Provider architecture scaffold
* Queue manager scaffold
* AI provider scaffold
* TypeScript interfaces
* Message passing setup
* Content script injection setup

Create clean scalable architecture suitable for future expansion.
